<%= form_with(model: user_task) do |form| %>
  <% if user_task.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_task.errors.count, "error") %> prohibited this user_task from being saved:</h2>

      <ul>
        <% user_task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :task_id, style: "display: block" %>
    <%= form.text_field :task_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
